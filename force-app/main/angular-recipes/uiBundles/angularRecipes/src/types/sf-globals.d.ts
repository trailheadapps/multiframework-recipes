/** Build-time API version, injected by @salesforce/angular-plugin-ui-bundle. */
declare const __SF_API_VERSION__: string;

/** `?shiki` imports resolve to a file's pre-highlighted source HTML. */
declare module '*?shiki' {
	const html: string;
	export default html;
}
